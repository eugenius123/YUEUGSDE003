To run file via bash or cmd prompt:

1. Python3.6 used for testing
2. Install libraries first: pip install robobrowser requests BeautifulSoup4 (Mac os).
3. Use pip3 to install if planning to use python3
4. Type "python3 expedia_ui.py" within bash terminal to run script.
5. Output file will be in put in current directory.

